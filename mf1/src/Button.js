import React from "react";
import useFetch from './GetAppointments';

const Button = () =>  { 
  // const data = useFetch();
  // console.log(data);

  return (
  <button>MFE1 Button</button>
) };

export default Button;
